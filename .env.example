# Copy this file to .env and fill in real values. .env is gitignored — never commit it.

# console.anthropic.com > API Keys
ANTHROPIC_API_KEY=

# Supabase project settings > API
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Any long random string — required header (X-Admin-Secret) to trigger
# POST /api/synthesize-ideas. Placeholder until real admin auth exists.
ADMIN_SECRET=
